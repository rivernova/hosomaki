# Hosomaki configuration
# Copy this file to ~/.config/hosomaki/config.yaml and edit as needed.

ai:
  provider: ollama                     # currently the only supported provider
  endpoint: http://localhost:11434     # Ollama default
  model: llama3.2:3b                     # any model you have pulled: gemma3, mistral, qwen3…
  timeout: 120s                        # increase for slow hardware or large models

output:
  color: true
  language: en                         # language for LLM responses (en, es, fr, de, …)